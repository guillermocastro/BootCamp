@model IEnumerable<BootCamp.Models.Author>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AuthorMail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuthorReview)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AuthorMail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuthorReview)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AuthorName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AuthorName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AuthorName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
